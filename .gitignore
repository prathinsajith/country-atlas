# Dependencies
node_modules
.npm

# Build
dist
.tsbuildinfo

# Testing
coverage
.nyc_output

# Environment / Local
.env
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Data Pipelines (Reproducible via scripts)
# We ignore raw downloads and intermediate flags, but keep normalized data if intended for repo
# For this project, let's keep 'normalized' so the repo works out of the box, but ignore the rest.
data/raw
data/flags

# Editor
.vscode
.idea
